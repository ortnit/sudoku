# CLAUDE.md

Read and follow `AGENTS.md` first. This file only adds Claude-specific reminders for `ortnit/sudoku`.

## Project

- Repository: `ortnit/sudoku`
- Purpose: small Python Sudoku solver.

## Working rules

- Keep the solver small and understandable.
- Prefer tests or executable examples over prose that restates implementation details.
- Do not duplicate current Python versions, dependency lists, or behavior details in prose; use code and manifests as authority.
- Do not install dependencies or release unless explicitly delegated.
- After verification, make small, traceable commits and push directly when the repository has a safe upstream.
